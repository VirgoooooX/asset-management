# 🔬 Chamber Tracker | 设备资产管理平台

![Version](https://img.shields.io/badge/version-0.1.8-blue) ![React](https://img.shields.io/badge/React-18-61DAFB?logo=react&logoColor=black) ![TypeScript](https://img.shields.io/badge/TypeScript-5.0-3178C6?logo=typescript&logoColor=white) ![Vite](https://img.shields.io/badge/Vite-5.0-646CFF?logo=vite&logoColor=white) ![Docker](https://img.shields.io/badge/Docker-Ready-2496ED?logo=docker&logoColor=white)

> **全生命周期管理** | **可视化监控** | **自动化对账**
>
> 一个现代化、前后端一体的实验室设备资产管理平台。基于 React + TypeScript + Express 构建，旨在解决设备状态追踪难、校准过期遗漏、使用记录混乱等痛点。

---

## ✨ 核心功能 (Core Features)

### 📊 1. 智能仪表盘 & 异常监控
实时掌握实验室资产健康度。系统自动计算 KPI，并监控异常状态。
- **状态概览**：可用/使用中/维护中/校准过期设备分布。
- **自动告警**：校准到期提醒、超长占用告警（阈值可配置）、逾期归还提醒。
- **数据导出**：支持 Excel 格式导出完整使用记录，便于归档与二次分析。

> *[此处建议放置 Dashboard 页面截图]*
> `docs/images/dashboard-preview.png`

### 📅 2. 可视化时间线 (Interactive Timeline)
告别枯燥的表格，用甘特图视角管理设备占用。
- **直观视图**：按设备维度的甘特图展示，空闲/忙碌时段一目了然。
- **快捷操作**：支持直接在时间线上点击创建、查看或结束使用记录。
- **状态联动**：时间线操作自动同步更新设备状态。

> *[此处建议放置 Timeline 页面截图]*
> `docs/images/timeline-preview.png`

### 🛠️ 3. 全流程维修与校准管理
闭环管理设备的维护生命周期。
- **维修工单**：支持“询价 -> 待维修 -> 维修中 -> 完成”的完整流转。
- **状态自动化**：维修开始/结束自动变更设备状态，无需手动二次操作。
- **校准追踪**：记录上次与下次校准日期，到期前自动在 Dashboard 提示。

---

## 🏗️ 技术架构 (Tech Stack)

| 领域 | 技术栈 | 说明 |
| :--- | :--- | :--- |
| **前端** | **React 18** + TypeScript | 强类型组件化开发 |
| **构建** | **Vite 5** | 极速冷启动与 HMR |
| **UI 库** | **MUI v5** (Material UI) | 企业级设计系统，含 `@mui/x-date-pickers` |
| **状态管理** | **Redux Toolkit** | 集中式状态管理，处理复杂的异步逻辑 |
| **后端** | **Express** + Node.js | 轻量级 RESTful API |
| **数据库** | **SQLite** | 零配置、文件级数据库，易于备份与迁移 |
| **部署** | **Docker** | 容器化一键部署 |

---

## 🚀 快速开始 (Quick Start)

### 方式一：Docker 一键部署 (推荐)

如果您已安装 Docker 和 Docker Compose，这是最快的运行方式。

```bash
# 启动服务 (后台运行)
docker-compose up -d

# 访问应用
# 浏览器打开 http://localhost:3000
```

### 方式二：本地开发模式

适合需要修改代码或进行二次开发的场景。

#### 1. 安装依赖

```bash
npm install
```

#### 2. 启动开发服务器

```bash
# 同时启动前端 Vite 和后端 Express 服务
npm run dev
```

#### 3. 构建与检查

```bash
# 类型检查
npm run typecheck

# 生产环境构建
npm run build

# 运行测试
npm run test
```

---

## 📂 项目结构

```text
l:\Web\Chamber tracker\
├── src/
│   ├── components/  # 通用 UI 组件 (AppCard, Layout, etc.)
│   ├── pages/       # 业务页面 (Dashboard, Timeline, Chambers...)
│   ├── services/    # API 请求与业务服务层
│   ├── store/       # Redux 状态管理 (Slices & Selectors)
│   ├── utils/       # 工具函数 (状态判定, Excel 导出)
│   └── types.ts     # TypeScript 类型定义
├── backend/         # Express 后端源码
├── public/          # 静态资源
└── docs/            # 项目文档与截图
```

---

## 👩‍💻 开发者指南

### 📐 布局规范 (Layout Stability)
为了防止页面切换时出现“左右抖动”（由于滚动条的出现/消失）：
*   **滚动容器**：统一由 [Layout.tsx](src/components/Layout.tsx) 中的 `.app-scroll` 控制纵向滚动。
*   **样式技巧**：使用了 `scrollbar-gutter: stable` 预留滚动条空间。
*   **注意**：请勿在具体的 Page 组件中添加全屏滚动容器。

### 🔄 状态对账机制 (Reconciliation)
本项目的一大特色是**使用记录驱动设备状态**。
*   **判定逻辑**：`isUsageLogOccupyingAsset` 函数 ([statusHelpers.ts](src/utils/statusHelpers.ts)) 用于判断某条记录是否正在占用设备。
*   **自动服务**：`reconcileAssetStatusesFromUsageLogs` 服务 ([assetStatusReconcileService.ts](src/services/assetStatusReconcileService.ts)) 会在拉取记录后自动校正设备显示的当前状态。

---

## 📄 License

Private Repository.
